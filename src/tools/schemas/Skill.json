{
  "type": "object",
  "properties": {
    "command": {
      "type": "string",
      "enum": ["load", "unload"],
      "description": "The operation to perform: \"load\" to load skills, \"unload\" to unload skills"
    },
    "skills": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of skill IDs to load or unload (e.g., [\"data-analysis\", \"web-scraper\"])"
    }
  },
  "required": ["command", "skills"],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
