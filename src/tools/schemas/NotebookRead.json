{
  "type": "object",
  "properties": {
    "notebook_path": {
      "type": "string",
      "description": "Path to the .ipynb notebook file."
    },
    "cell_index": {
      "type": "number",
      "description": "Read a specific cell by index (0-based). If omitted, reads all cells."
    },
    "include_outputs": {
      "type": "boolean",
      "description": "Whether to include cell outputs. Defaults to true."
    }
  },
  "required": ["notebook_path"],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
