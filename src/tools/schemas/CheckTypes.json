{
  "type": "object",
  "properties": {
    "file_path": {
      "type": "string",
      "description": "The absolute path to the TypeScript/JavaScript file to analyze"
    },
    "symbol_name": {
      "type": "string",
      "description": "Optional: Name of a specific symbol (function, class, interface, variable) to get type information for"
    },
    "validate": {
      "type": "boolean",
      "description": "Optional: If true, runs full type checking on the file and returns any type errors found"
    }
  },
  "required": ["file_path"],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
