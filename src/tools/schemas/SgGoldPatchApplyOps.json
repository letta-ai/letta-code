{
  "type": "object",
  "properties": {
    "proposals_path": {
      "type": "string",
      "description": "Path to the proposals JSON file to patch (absolute or relative to the current working directory)."
    },
    "source_path": {
      "type": "string",
      "description": "Optional transcript source path used for validation (absolute or relative to the current working directory)."
    },
    "ops": {
      "type": "array",
      "description": "GoldProposalsPatchOps v1 ops array (objects with op = '...').",
      "items": {
        "type": "object"
      }
    }
  },
  "required": ["proposals_path", "ops"],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
