{
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Repository name. Must contain only letters, numbers, dots, underscores, and hyphens. Mounted at $MEMORY_DIR/../<name>."
    }
  },
  "required": ["name"],
  "additionalProperties": false,
  "$schema": "http://json-schema.org/draft-07/schema#"
}
